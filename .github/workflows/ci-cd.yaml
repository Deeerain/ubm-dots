name: Simple Arch Build

on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: 🐋 Build Docker image
      run: docker build -t ubm-dots-builder .
      
    - name: 🏗️ Run build
      run: |
        docker run --rm \
          -v "$(pwd):/home/builder/ubm-dots" \
          ubm-dots-builder
          
    - name: 🚀 Create release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: "*.pkg.tar.zst"
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
